@model IEnumerable<BookShop.Models.Author>

@{
    ViewData["Title"] = "Authors";
}

<div class="container-fluid py-4 px-md-4">
    <!-- Header -->
    <div class="d-flex flex-wrap justify-content-between align-items-center mb-4 pb-2 border-bottom">
        <div>
            <h1 class="h3 fw-bold mb-1 text-dark">
                <i class="bi bi-pen-fill text-primary me-2"></i>Author Management
            </h1>
            <p class="text-muted mb-0">Manage book authors and contributor biographies</p>
        </div>
        <div class="mt-3 mt-md-0">
            <a asp-action="Create" class="btn btn-primary shadow-sm">
                <i class="bi bi-person-plus-fill me-1"></i>Add New Author
            </a>
        </div>
    </div>

    <!-- Alerts -->
    @if (TempData["success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i>@TempData["success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["error"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show shadow-sm" role="alert">
            <i class="bi bi-exclamation-triangle-fill me-2"></i>@TempData["error"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- Table Card -->
    <div class="card border-0 shadow-sm rounded-3">
        <div class="card-header bg-white py-3">
            <h5 class="mb-0 fw-semibold">All Authors</h5>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table class="table table-hover align-middle mb-0">
                    <thead class="table-light text-secondary text-uppercase small">
                        <tr>
                            <th scope="col" class="ps-4">Author Name</th>
                            <th scope="col">Biography Preview</th>
                            <th scope="col">Books Count</th>
                            <th scope="col" class="text-end pe-4">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (!Model.Any())
                        {
                            <tr>
                                <td colspan="4" class="text-center py-4 text-muted">
                                    No authors found. Click "Add New Author" to create one.
                                </td>
                            </tr>
                        }
                        else
                        {
                            @foreach (var author in Model)
                            {
                                <tr>
                                    <td class="ps-4">
                                        <div class="d-flex align-items-center">
                                            <div class="rounded-circle bg-primary bg-opacity-10 text-primary d-flex align-items-center justify-content-center me-3" style="width: 38px; height: 38px;">
                                                <i class="bi bi-person-lines-fill"></i>
                                            </div>
                                            <span class="fw-semibold text-dark">@author.Name</span>
                                        </div>
                                    </td>
                                    <td>
                                        <span class="text-muted small">
                                            @(string.IsNullOrWhiteSpace(author.Biography) 
                                                ? "No biography provided." 
                                                : (author.Biography.Length > 90 ? author.Biography.Substring(0, 90) + "..." : author.Biography))
                                        </span>
                                    </td>
                                    <td>
                                        <span class="badge bg-primary bg-opacity-10 text-primary border border-primary px-2 py-1">
                                            <i class="bi bi-book me-1"></i>@(author.Books?.Count ?? 0) Books
                                        </span>
                                    </td>
                                    <td class="text-end pe-4">
                                        <div class="btn-group" role="group">
                                            <a asp-action="Edit" asp-route-id="@author.Id" class="btn btn-sm btn-outline-primary" title="Edit Author">
                                                <i class="bi bi-pencil-square me-1"></i>Edit
                                            </a>
                                            <form asp-action="Delete" asp-route-id="@author.Id" method="post" class="d-inline">
                                                <button type="submit" class="btn btn-sm btn-outline-danger ms-1" title="Delete Author"
                                                        onclick="return confirm('Are you sure you want to delete author \'@author.Name\'?');">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
